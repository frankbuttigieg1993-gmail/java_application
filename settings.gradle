rootProject.name = 'env-printer-springboot-project'

buildCache {
    local {
        enabled = true
    }
}

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}