rootProject.name = 'env-printer-springboot-project'
pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}
buildCache {
    local {
        enabled = true
    }
}

