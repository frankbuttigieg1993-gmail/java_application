pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'env-printer-springboot-project'

buildCache {
    local {
        enabled = true
    }
}

