name: Build Pipeline
run-name: ${{ github.ref_name }}.${{ github.run_number }}

on:
  push:
    branches:
      - main
      - 'release/**'
  workflow_dispatch:

jobs:
  build:
    name: '-'
    uses: frankbuttigieg1993-gmail/cicd/.github/workflows/java-build-all.yml@main
    permissions:
      id-token: write
      contents: write 
      security-events: write
      actions: write
      issues: write
      pull-requests: write
      statuses: write
    with:
      target-runner: "['self-hosted', 'linux', 'x64']"
    secrets: inherit