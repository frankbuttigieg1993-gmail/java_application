name: PR Pipeline
run-name: ${{ github.ref_name }}.${{ github.run_number }}

on:
  pull_request:
    branches: [ release/* ]
  workflow_dispatch:

jobs:
  build:
    name: '-'
    uses: frankbuttigieg1993-gmail/cicd/.github/workflows/java-build-pr.yml@main
    permissions:
      id-token: write
      contents: write 
      security-events: write
    with:
      target-runner: "['self-hosted', 'linux', 'x64']"
    
